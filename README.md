# Simple Static Server

A lightweight, zero-dependency static file server built with Node.js. Perfect for local development, testing, and serving static websites.

## 🚀 Features

- **Zero Dependencies** - Pure Node.js implementation
- **Lightweight** - Minimal footprint and fast performance
- **Easy to Use** - Simple command-line interface
- **Configurable** - Customizable port and directory serving
- **CORS Enabled** - Cross-origin resource sharing support
- **MIME Type Support** - Automatic content type detection
- **Directory Listing** - Optional directory index views

## 📦 Installation

### Prerequisites
- Node.js (v12 or higher)

### Quick Start
```bash
# Clone the repository
git clone https://github.com/vzl-rbi/simple-static-server.git
cd simple-static-server

# Run directly with Node
node server.js
```

### Global Installation (Optional)
```bash
# Install globally for easy access
npm install -g .

# Now you can run from anywhere
simple-static-server
```
## 📁 Project Structure

```
simple-static-server/
├── server.js          # Main server implementation
├── package.json       # Project configuration
├── README.md          # Documentation
└── (your static files)# Files to be served
```




